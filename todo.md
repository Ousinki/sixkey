# SixKey TODO

## RIME 输入法

- [ ] **文转盲声调输入**：数字键 1-4 被 RIME 用于候选词选择，无法直接输入声调盲文符号。可能方案：
  - 方案 A：用分号前缀 `;1` `;2` `;3` `;4`
  - 方案 B：用字母编码 `sd1` `sd2` `sd3` `sd4`
- [ ] **o/e 歧义处理**：并击方案中 o 和 e 共用 dots 2,6，默认输出 `e`，通过 speller derive 规则处理 bo/po/mo/fo。需验证实际使用体验
- [ ] **界音声调**：声母独用 + 韵母独用之间需加界音声调（如 植物 → ⠌⠂⠥）。插件已自动实现，但输入法中因不知道声调信息，需依赖用户手动输入。前置条件：先解决声调输入问题
